.DS_Store

# Synced source tree from ../ha-pulse (regenerated by sync.sh).
# Add-on-specific files (config.yaml, README.md, icon.png, logo.png) live
# alongside but are NOT excluded — they're committed.
pulse/apps/
pulse/packages/
pulse/package.json
pulse/pnpm-lock.yaml
pulse/pnpm-workspace.yaml
pulse/tsconfig.base.json
pulse/biome.json
pulse/Dockerfile
pulse/.dockerignore
